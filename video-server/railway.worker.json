{
  "$schema": "https://railway.app/railway.schema.json",
  "_comment": "Railway에서 워커 프로세스를 별도 Service로 추가할 때 이 파일을 railway.json으로 지정하거나 Service Settings에서 startCommand를 'node worker.js'로 설정하세요. web service와 동일한 repo/Dockerfile을 공유하고 Redis addon(REDIS_URL)과 Supabase env만 주입하면 됩니다.",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "node worker.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
